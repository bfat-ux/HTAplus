# Environment setup

Create a `.env` file in this folder with your SMTP credentials:

```
EMAIL=your_email@gmail.com
PASS=your_app_password
CORS_ORIGIN=http://localhost:5173
```

For Gmail, use an App Password if 2FA is enabled.
